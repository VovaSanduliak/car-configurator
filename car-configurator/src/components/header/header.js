import Nav from "../nav/nav";
import Logo from "../logo/logo";

import "./header.css";

const Header = () => {
  return (
    <header style={{}}>
      <Logo />
      <Nav />
    </header>
  );
};

export default Header;
